% Prelude in C major — J.S. Bach, BWV 846 (WTC I) (public domain composition)
% 34 bars, two hands, broken-chord figuration. ~72 BPM.
%
% PHASE-0 STUB. The prototype exported this from the music21 corpus, NOT a Mutopia .ly
% (see PLAN.md §5). Decision to record in LICENSES.md: either typeset a simple standalone
% .ly here, or keep prelude-in-c as a golden-MIDI-only piece initially.
% Prototype build script: ../../music/make_midi.py / ../../music/minuet.py lineage.
